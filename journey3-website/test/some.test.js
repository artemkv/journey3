'use strict';

const QUnit = require('qunit');

// TODO:
QUnit.test('test', function(assert) {
    const expectedText = 'aaa';
    const actualText = 'aaa';

    assert.equal(actualText, expectedText, 'todo');
});
